import * as react_jsx_runtime from 'react/jsx-runtime';
import { ReactNode } from 'react';

type Mode = "system" | "dark" | "light";
type IDarkModeContext = {
    mode: Mode;
    setMode: (value: Mode) => void;
};
type Props = {
    children?: ReactNode;
};
declare const DarkModeProvider: ({ children }: Props) => react_jsx_runtime.JSX.Element;
declare const useDarkMode: () => IDarkModeContext;

export { DarkModeProvider, type IDarkModeContext, type Mode, useDarkMode };
